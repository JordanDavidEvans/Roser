[gd_scene load_steps=3 format=3 uid="uid://dyt158hux0wyu"]

[ext_resource type="Texture2D" uid="uid://cbcjoeg3gii7h" path="res://materials/arm.png" id="1_xpjvk"]

[sub_resource type="GDScript" id="GDScript_yfsg5"]
script/source = "extends Sprite2D

func _process(delta):
	# Get the mouse position relative to the game window
	var mouse_pos = get_global_mouse_position()

	# Calculate the angle to rotate
	var angle_to_mouse = (mouse_pos - position).angle()

	# Set the object's rotation to face the mouse cursor
	rotation = angle_to_mouse

	# Calculate the distance to the mouse cursor
	var distance_to_mouse = position.distance_to(mouse_pos)
	scale.y = distance_to_mouse

	# Optionally, you can print the distance to the output console for debugging
	print(\"Distance to mouse: \", distance_to_mouse)
"

[node name="handscene" type="Node2D"]
rotation = 1.5708
metadata/_edit_group_ = true

[node name="hand" type="Sprite2D" parent="."]
rotation = 1.5708
texture = ExtResource("1_xpjvk")
script = SubResource("GDScript_yfsg5")
